{
  "name": "barcode-analytics-dashboard",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "test": "vitest run",
    "typecheck": "nuxt prepare && vue-tsc --noEmit -p tsconfig.nuxt.json && tsc --noEmit -p tsconfig.scripts.json",
    "baklog": "tsx scripts/generate-baklog.ts"
  },
  "dependencies": {
    "@nuxt/ui": "^2.22.0",
    "nuxt": "^3.17.5",
    "vue": "^3.5.16",
    "vue-router": "^4.5.1"
  },
  "devDependencies": {
    "@types/node": "^25.6.2",
    "tsx": "^4.21.0",
    "typescript": "^5.8.3",
    "vitest": "^3.1.4",
    "vue-tsc": "^2.2.10"
  }
}
